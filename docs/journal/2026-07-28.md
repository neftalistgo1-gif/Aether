# Journal - 2026-07-28

## Objetivos

- Comenzar el módulo Customers.
- Alinear el esquema de clientes con el modelo de dominio.
- Verificar el funcionamiento de las primeras rutas del módulo.

## Logros

- Se separaron los datos personales de Customer de los datos de Service.
- Se implementaron rutas para crear, listar y consultar clientes.
- Se añadió almacenamiento temporal en memoria hasta integrar PostgreSQL.
- Se registró el router de Customers en la aplicación principal.
- Se añadieron pruebas automatizadas para el flujo básico y el error 404.
- Se declararon las dependencias del backend en `pyproject.toml`.
- Se configuró `.gitignore` para excluir entornos, cachés y archivos locales.
- Se reemplazó el almacenamiento temporal por persistencia con SQLAlchemy.
- Se creó la primera migración de Alembic para la tabla `customers`.
- Se preparó PostgreSQL 18 para desarrollo local mediante Docker Compose.
- Las pruebas ahora comprueban que un cliente permanece entre sesiones.
- Se levantó PostgreSQL 18.4 correctamente mediante Docker Desktop.
- Se aplicó la primera migración sobre la base de datos real.
- Se verificó por HTTP la creación y consulta de un cliente desde la API.
- Se añadió actualización parcial de clientes mediante `PATCH`.
- Se añadió búsqueda por nombre o teléfono mediante el parámetro `q`.
- Se ampliaron las pruebas automatizadas de Customers de dos a seis.
- Se creó el módulo Service con código AMR, domicilio, plan, precio acordado,
  día de pago, tolerancia y estado.
- Se creó ServiceHolder para conservar la relación histórica entre personas
  y servicios.
- Se aplicó la segunda migración en PostgreSQL.
- Se añadieron cuatro pruebas de Service; el proyecto suma diez pruebas.
- Se verificó el flujo real de creación, consulta y búsqueda de un servicio.

## Lecciones aprendidas

- Customer representa a una persona y no debe contener IP, torre, AP o plan.
- APIRouter permite agrupar rutas con un prefijo y una etiqueta comunes.
- Los modelos de entrada y salida pueden ser diferentes.
- El almacenamiento en memoria sirve para validar la API, pero pierde sus
  datos al reiniciar y no sustituye una base de datos.
- Los modelos ORM representan las tablas y los esquemas Pydantic representan
  el contrato público de la API.
- Alembic permite evolucionar el esquema sin recrear manualmente la base.

## Próximo objetivo

Probar Service desde Swagger con un cliente existente. Después se añadirá la
actualización controlada del servicio y su ciclo de activación o cancelación.
