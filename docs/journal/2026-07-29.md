# Diario de desarrollo — 2026-07-29

## Módulo 28: gestión de clientes desde la UI

- Se habilitó el alta de clientes desde `/app/` para operadores con la
  capacidad `customers.write`.
- Se añadió edición de nombre, teléfonos, correo y notas mediante un formulario
  accesible y adaptable a pantallas pequeñas.
- Las acciones de escritura permanecen ocultas para cuentas sin permiso.
- La interfaz compara el formulario con el registro original y evita enviar
  actualizaciones vacías.
- Toda modificación exige un motivo operativo.
- El backend registra auditoría inmutable para altas y ediciones, con valores
  anteriores y posteriores.
- Los errores de validación de la API se traducen en mensajes visibles dentro
  del formulario.
- No se añadieron dependencias ni cambios de base de datos.
- Las ciento cuarenta y seis pruebas están aprobadas, incluida la protección
  contra actualizaciones sin cambios reales.

## Módulo 29: alta de servicios desde la UI

- Se añadió el formulario de alta para cuentas con `services.write`.
- El operador selecciona un titular existente y un plan activo en lugar de
  capturar nombre o precio libremente.
- El backend valida que `plan_id`, nombre y precio mensual coincidan con el
  catálogo vigente.
- Se conservó compatibilidad de lectura y creación programática para servicios
  históricos que todavía no tienen `plan_id`.
- Cada alta genera titular inicial, evento de registro y auditoría inmutable.
- Los servicios nuevos permanecen pendientes; el formulario no omite el flujo
  de instalación ni activa conexiones.
- La UI exige código AMR válido, domicilio, día de pago, tolerancia y motivo.
- No se requirió migración porque `services.plan_id` ya existía.
- Las ciento cuarenta y siete pruebas están aprobadas.

## Módulo 30: recepción segura de pagos desde la UI

- Se añadió el formulario de recepción para cuentas con `billing.write`.
- El operador selecciona al cliente y, de forma opcional, uno de sus servicios
  visibles; también registra monto, fecha, medio y referencias operativas.
- Todo pago creado desde la interfaz conserva el estado `pending`.
- La pantalla aclara que registrar el pago no reduce deuda y mantiene fuera de
  este flujo la verificación y la aplicación.
- La referencia de almacenamiento del comprobante permanece en la base de
  datos, pero se retiró de las respuestas públicas.
- La API expone únicamente `has_proof` para indicar si existe evidencia.
- Cada recepción continúa generando evento de estado y auditoría inmutable.
- No se requirió migración de base de datos.
- Las ciento cuarenta y ocho pruebas están aprobadas y Alembic confirma que el
  esquema permanece sincronizado.

## Módulo 31: aprobación y aplicación de pagos desde la UI

- Se añadieron acciones visibles únicamente para cuentas con
  `billing.approve`.
- Los pagos pendientes pueden verificarse, rechazarse o cancelarse sin borrar
  su historial.
- La interfaz exige explicación cuando el monto confirmado difiere del
  declarado y para toda decisión de rechazo o cancelación.
- Verificar un pago no reduce deuda; el operador recibe una advertencia clara
  de que todavía debe aplicarlo.
- Los pagos verificados y no aplicados ofrecen una segunda confirmación para
  distribuir el monto entre los cargos abiertos más antiguos.
- La interfaz informa cuánto se aplicó a deuda y cuánto se convirtió en saldo a
  favor.
- Se corrigió la política del backend para que aplicar un pago también exija
  `billing.approve`, cerrando un posible atajo mediante `billing.write`.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas están aprobadas y Alembic no detecta
  cambios pendientes.

## Módulo 32: estado de cuenta por cliente

- Se añadió un resumen autoritativo por cliente con deuda total, deuda vencida,
  número de cargos abiertos y saldo a favor.
- El endpoint admite una fecha de corte para mantener cálculos históricos
  reproducibles.
- La interfaz ofrece “Estado de cuenta” únicamente a cuentas con
  `billing.read`.
- El detalle muestra cada cargo, vencimiento, importe original, monto pendiente
  y estado sin permitir mutaciones.
- Las fechas sin hora se interpretan como fechas locales para evitar que el
  navegador muestre el día anterior por conversión de zona horaria.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas continúan aprobadas y Alembic confirma
  que el esquema está sincronizado.

## Módulo 33: catálogo de planes desde la UI

- Se añadió una sección de planes visible para cuentas con `plans.read`.
- Las cuentas con `plans.write` pueden registrar una oferta con nombre,
  velocidad, descripción, tarifa inicial, vigencia y motivo.
- Los cambios de tarifa se realizan mediante el endpoint especializado y
  preservan todos los periodos de precio.
- La interfaz impide ofrecer un cambio de tarifa el mismo día de creación,
  respetando que la nueva vigencia debe comenzar después de la actual.
- Desactivar un plan exige confirmación y motivo; sólo lo retira de nuevas
  altas.
- Los servicios existentes conservan plan y precio acordado después de cambiar
  o desactivar una oferta.
- El alta de servicios sigue mostrando únicamente planes activos con precio
  vigente.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic confirma
  que no hay cambios de esquema pendientes.

## Módulo 34: cobertura y agenda de instalación desde la UI

- Se añadió la acción de evaluación para servicios pendientes y cuentas con
  `installations.write`.
- La cobertura puede registrarse como viable, con equipo especial o fuera de
  cobertura.
- El equipo especial exige descripción y los resultados viables exigen fecha
  programada.
- Una evaluación fuera de cobertura fuerza costo cero y no crea agenda ni
  cargo.
- Una instalación viable con costo genera su cargo mediante la regla
  autoritativa del backend.
- Antes de abrir el formulario, las cuentas con `installations.read` comprueban
  si ya existe trabajo programado para evitar duplicados.
- El módulo no permite completar ni activar el servicio; esas acciones
  conservarán pago, fotografías y navegación como requisitos independientes.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic no detecta
  cambios pendientes.

## Módulo 35: reprogramación y cancelación de instalaciones

- La UI detecta el trabajo programado antes de ofrecer una nueva evaluación.
- El detalle muestra fecha, costo y existencia del cargo relacionado.
- Reprogramar exige una fecha distinta y un motivo, conserva el historial y
  mueve el vencimiento del cargo mediante la regla del backend.
- Cancelar exige motivo y conserva tanto la instalación como su auditoría.
- El cargo se cancela junto con el trabajo únicamente cuando no tiene pagos
  aplicados.
- Ninguna de estas acciones completa ni activa el servicio.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic confirma
  que el esquema sigue sincronizado.

## Módulo 36: cierre técnico y activación segura

- Se añadió la finalización de instalaciones programadas desde la UI.
- El cierre exige entre uno y tres técnicos, dos a cuatro evidencias de antena
  y una a cuatro de módem.
- La confirmación de navegación y la persona que la proporcionó son
  obligatorias.
- La API mantiene el bloqueo cuando el cargo relacionado no está pagado.
- Sólo una instalación inicial completada cambia el servicio de pendiente a
  activo y registra la fecha de activación.
- Las rutas privadas de fotografías ya no aparecen en respuestas de la API;
  sólo se publican los conteos de evidencia.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic no detecta
  cambios pendientes.

## Módulo 37: simulación segura de control MikroTik

- Se añadió una acción para cuentas con `network.control` en servicios activos
  o suspendidos.
- La UI selecciona suspensión o reactivación según el estado comercial actual.
- Cada solicitud genera una clave de idempotencia única.
- La interfaz fuerza `dry_run: true`; no ofrece ejecución real.
- La simulación valida que exista asignación IP vigente y un router registrado.
- El comando queda auditado, pero no cambia la address-list ni el estado del
  servicio.
- La suspensión comercial coordinada continúa fuera de este módulo porque
  requiere deuda vencida, tolerancia, prórroga y notificación entregada.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic confirma
  que no hay cambios de esquema pendientes.

## Módulo 38: notificaciones verificables desde la UI

- Se añadió el registro de comunicaciones para cuentas con
  `notifications.write`.
- La UI contempla avisos de suspensión, recordatorios de pago, actualizaciones
  de servicio y mensajes generales.
- Se registran WhatsApp, SMS, correo, llamada y comunicación presencial.
- Cada intento conserva resultado, destinatario, hora, resumen y responsable.
- Las entregas digitales exigen referencia del proveedor o evidencia privada.
- Los intentos fallidos exigen un motivo de al menos tres caracteres; esta
  validación se reforzó también en el backend.
- Las referencias privadas de evidencia no aparecen en las respuestas.
- Un aviso de suspensión entregado queda disponible para el futuro flujo
  comercial coordinado.
- No se requirió migración de base de datos.
- Las ciento cuarenta y nueve pruebas permanecen aprobadas y Alembic confirma
  que el esquema sigue sincronizado.

## Módulo 39: simulación comercial coordinada de suspensión

- Se añadió una validación segura para servicios activos.
- La UI consulta deuda total, deuda vencida y cargos abiertos directamente de
  la API.
- El operador selecciona un aviso de suspensión entregado al titular actual.
- Se exige confirmar que terminó la tolerancia y que se revisaron prórrogas.
- El backend recalcula deuda, mensualidades vencidas, tolerancia, prórrogas,
  titular y uso previo de la notificación.
- La solicitud coordinada fuerza `dry_run: true` y genera una clave idempotente.
- Una simulación aprobada no modifica MikroTik ni el estado comercial.
- La política se corrigió para exigir `network.control` en suspensión y
  reactivación coordinadas; `services.write` ya no permite ese atajo.
- No se requirió migración de base de datos.
- Las ciento cincuenta pruebas permanecen aprobadas y Alembic confirma que el
  esquema sigue sincronizado.

## Módulo 40: simulación comercial coordinada de reactivación

- Se añadió una validación segura para servicios suspendidos.
- La acción sólo aparece con `network.control` y `billing.read`.
- La UI consulta deuda total, deuda vencida y cargos abiertos desde la API.
- Se exige registrar quién autorizó la reactivación y el motivo o acuerdo
  aplicable.
- El backend recalcula la deuda y rechaza cualquier saldo declarado que no
  coincida con los cargos abiertos de Aether.
- La solicitud coordinada fuerza `dry_run: true` y utiliza una clave de
  idempotencia única.
- Una simulación aprobada no modifica MikroTik ni reactiva el servicio.
- No se requirió migración de base de datos.
- Las ciento cincuenta y una pruebas permanecen aprobadas y Alembic confirma
  que el esquema sigue sincronizado.

## Módulo 41: prórrogas auditables desde la UI

- Se añadió consulta del historial de prórrogas por servicio.
- La pantalla muestra deuda actual y si existe una prórroga vigente.
- `billing.write` permite registrar una nueva prórroga sólo con deuda abierta
  y sin otra vigente.
- La fecha original de pago ya no puede estar en el futuro y la fecha prometida
  debe ser posterior.
- La UI conserva motivo, autorización, notas y una referencia privada de
  evidencia.
- Las respuestas sólo exponen `has_evidence`; la ubicación interna dejó de
  publicarse.
- Cumplir o cancelar una prórroga requiere `billing.approve`; antes, cumplirla
  podía ejecutarse indebidamente con `billing.write`.
- Resolver una prórroga conserva todo el historial.
- No se requirió migración de base de datos.
- Las ciento cincuenta y tres pruebas permanecen aprobadas y Alembic confirma
  que el esquema sigue sincronizado.

## Módulo 42: convenios de pago flexibles

- Se añadió `PaymentAgreement` con folio, titular, servicio, términos,
  autorización, estado y resolución.
- Monto prometido, fecha, parcialidades, evidencia y notas son opcionales para
  no inventar información que no fue acordada.
- El convenio requiere deuda abierta.
- Un monto opcional no puede superar el saldo calculado por Aether.
- Una fecha prometida opcional no puede estar en el pasado.
- La referencia de evidencia permanece privada; la API sólo publica
  `has_evidence`.
- Los convenios pueden marcarse como cumplidos o cancelados y esos estados son
  terminales.
- Crear requiere `billing.write`; resolver requiere `billing.approve`.
- Se añadió la migración `20260729_27`.
- La UI queda deliberadamente para el siguiente módulo, después de validar la
  nueva persistencia en PostgreSQL.
- Las ciento cincuenta y nueve pruebas permanecen aprobadas.
- La migración quedó aplicada en PostgreSQL y Alembic confirma
  `20260729_27 (head)` sin cambios de esquema pendientes.

## Módulo 43: convenios flexibles desde la UI

- Se añadió consulta de convenios e historial por servicio.
- La pantalla muestra deuda actual y cantidad de convenios vigentes.
- `billing.write` habilita el alta sólo para servicios activos o suspendidos
  con deuda.
- Los términos y la autorización son obligatorios.
- Monto, fecha, parcialidades, evidencia y notas pueden dejarse vacíos.
- La UI limita el monto opcional al saldo informado por Aether y no acepta
  fechas prometidas anteriores al día actual.
- La evidencia sólo se presenta como existente o ausente; su referencia
  permanece privada.
- `billing.approve` permite seleccionar un convenio vigente y resolverlo como
  cumplido o cancelado.
- Registrar o resolver convenios no modifica deuda ni aplica pagos.
- No se requirió una migración adicional; continúa vigente `20260729_27`.
- Las ciento cincuenta y nueve pruebas permanecen aprobadas y Alembic confirma
  `20260729_27 (head)` sin cambios pendientes.

## Módulo 44: respaldo verificable para reactivaciones con deuda

- `Reactivation` conserva ahora `extension_id` o `payment_agreement_id`.
- Con deuda se exige exactamente un respaldo; con saldo cero se prohíbe
  adjuntar uno innecesario.
- El backend verifica que la deuda declarada coincida con los cargos abiertos.
- La prórroga o convenio debe estar vigente y pertenecer al mismo servicio y
  titular actual.
- El autorizante de la reactivación debe coincidir con el respaldo elegido.
- Los convenios con fecha prometida vencida no autorizan una reactivación.
- Los registros históricos anteriores a esta migración siguen siendo
  consultables aunque no cuenten con el nuevo vínculo.
- La UI consulta ambos historiales y sólo ofrece respaldos válidos.
- Al elegir un respaldo, el autorizante se completa desde el registro y queda
  bloqueado para edición.
- La simulación continúa forzada a `dry_run` y no cambia MikroTik ni el estado
  del servicio.
- Se añadió la migración `20260729_28`.
- Las ciento sesenta y cinco pruebas permanecen aprobadas.
- La migración quedó aplicada y Alembic confirma `20260729_28 (head)` sin
  cambios de esquema pendientes.

## Módulo 45: preflight obligatorio para control real de red

- Toda orden real de suspensión, reactivación o reconciliación debe enlazar una
  simulación independiente.
- El preflight debe coincidir con servicio, acción, asignación vigente, router,
  IP e intención de bloqueo.
- La simulación vence después de quince minutos y sólo puede autorizar una
  orden real.
- Las claves de idempotencia no pueden reutilizarse cambiando modo o preflight.
- Los reintentos conservan el modo original; una simulación no puede promoverse
  a ejecución real.
- Las rutas coordinadas de suspensión y reactivación aplican las mismas reglas.
- La UI permanece deliberadamente en modo de simulación hasta incorporar una
  confirmación explícita que presente el preflight y sus datos operativos.
- Se añadió la migración `20260729_29`.
- Las ciento setenta y una pruebas permanecen aprobadas.
- La migración quedó aplicada y Alembic confirma `20260729_29 (head)` sin
  cambios de esquema pendientes.

## Módulo 46: ejecución coordinada segura desde la UI

- La interfaz conserva el diagnóstico técnico como una simulación sin acceso a
  ejecución real.
- Suspensión y reactivación comercial utilizan ahora un flujo visible de dos
  etapas: simulación y confirmación final.
- La confirmación muestra código de servicio, acción e IP verificada.
- El operador debe escribir el código AMR y confirmar que revisó los datos.
- La ejecución reutiliza los datos comerciales ya validados y enlaza el
  preflight de un solo uso.
- La interfaz considera la simulación válida durante un máximo de quince
  minutos; el backend vuelve a verificar autoritativamente su vigencia.
- Si MikroTik no confirma el resultado, la UI no cambia el estado comercial.
- No se añadió lógica comercial al navegador: deuda, avisos, autorizaciones,
  asignación y resultado siguen siendo validados por la API.
- No se requirió una migración adicional; continúa vigente `20260729_29`.
- Las ciento setenta y dos pruebas permanecen aprobadas, la sintaxis del
  navegador es válida y Alembic no detecta cambios pendientes.
