# Journal - 2026-07-28

## Objetivos

- Comenzar el módulo Customers.
- Alinear el esquema de clientes con el modelo de dominio.
- Verificar el funcionamiento de las primeras rutas del módulo.

## Logros

- Se separaron los datos personales de Customer de los datos de Service.
- Se implementaron rutas para crear, listar y consultar clientes.
- Se añadió almacenamiento temporal en memoria hasta integrar PostgreSQL.
- Se registró el router de Customers en la aplicación principal.
- Se añadieron pruebas automatizadas para el flujo básico y el error 404.
- Se declararon las dependencias del backend en `pyproject.toml`.
- Se configuró `.gitignore` para excluir entornos, cachés y archivos locales.
- Se reemplazó el almacenamiento temporal por persistencia con SQLAlchemy.
- Se creó la primera migración de Alembic para la tabla `customers`.
- Se preparó PostgreSQL 18 para desarrollo local mediante Docker Compose.
- Las pruebas ahora comprueban que un cliente permanece entre sesiones.
- Se levantó PostgreSQL 18.4 correctamente mediante Docker Desktop.
- Se aplicó la primera migración sobre la base de datos real.
- Se verificó por HTTP la creación y consulta de un cliente desde la API.
- Se añadió actualización parcial de clientes mediante `PATCH`.
- Se añadió búsqueda por nombre o teléfono mediante el parámetro `q`.
- Se ampliaron las pruebas automatizadas de Customers de dos a seis.
- Se creó el módulo Service con código AMR, domicilio, plan, precio acordado,
  día de pago, tolerancia y estado.
- Se creó ServiceHolder para conservar la relación histórica entre personas
  y servicios.
- Se aplicó la segunda migración en PostgreSQL.
- Se añadieron cuatro pruebas de Service; el proyecto suma diez pruebas.
- Se verificó el flujo real de creación, consulta y búsqueda de un servicio.
- Se añadió actualización controlada de domicilio, plan, precio, día de pago
  y tolerancia.
- Se implementaron transiciones válidas entre pending, active, suspended y
  cancelled.
- Se creó un historial unificado de alta, cambios comerciales y cambios de
  estado con fecha y motivo.
- Se aplicó la tercera migración y se validó el ciclo completo en PostgreSQL.
- El proyecto suma doce pruebas automatizadas.
- Se implementaron registros operativos de suspensión y reactivación con
  deuda, responsables, notificación y resultado de MikroTik.
- Los intentos fallidos de red quedan registrados sin cambiar el estado.
- Se implementó la baja inmediata y programada con folio, saldos y estado de
  recuperación de equipos.
- Se restringieron los cambios directos de estado para obligar a usar los
  procesos operativos especializados.
- Se aplicó la cuarta migración y se validó el flujo real en PostgreSQL.
- El proyecto suma diecinueve pruebas automatizadas.
- Se implementó la programación y finalización de recuperación de equipos.
- Cada equipo esperado debe clasificarse como recuperado o faltante.
- El resultado completo, parcial o no recuperable se calcula automáticamente.
- Se conservan técnico, condición, evidencias y constancia de recepción.
- El estado de recuperación se sincroniza con la baja relacionada.
- Se aplicó la quinta migración y se validó una recuperación parcial real.
- El proyecto suma veintiséis pruebas automatizadas.
- Se documentó que la integración con MikroTik suspenderá agregando la IP del
  servicio a una address list y reactivará retirándola de esa lista.
- Se implementó la inspección de mantenimiento para cada equipo recuperado.
- Los equipos permanecen en cuarentena hasta registrar un dictamen.
- La liberación para reutilización exige limpieza y todas las pruebas
  aprobadas.
- Se conserva el historial de reparaciones y reinspecciones, y se bloquean
  cambios después de liberar o desechar un equipo.
- Se preparó la sexta migración y el proyecto suma treinta y dos pruebas.
- Se creó el inventario de activos con código interno permanente `AST-…`.
- Se implementó el historial de asignaciones de equipos a servicios.
- Se impidió asignar un equipo a dos servicios simultáneamente.
- Sólo servicios activos reciben equipos disponibles o listos para reutilizar.
- La recuperación reconoce activos existentes, cierra su asignación y los
  devuelve a cuarentena sin duplicarlos.
- La inspección actualiza el estado y los datos físicos del activo relacionado.
- Se aplicó la séptima migración y el proyecto suma treinta y siete pruebas.
- Se implementó NetworkAssignment con IP, router, torre, AP, nombre de antena,
  frecuencia, señal y técnico responsable.
- Cada cambio de red cierra la versión anterior y conserva el historial.
- Se impidió reutilizar simultáneamente una combinación vigente de router e IP.
- Se bloquearon cambios de red mientras el servicio está suspendido.
- La baja definitiva ahora cierra la configuración de red vigente.
- Se aplicó la octava migración y el proyecto suma cuarenta y tres pruebas.
- Se implementó Charge para instalación, mensualidad, cambios de domicilio,
  venta de equipos, servicios adicionales, ajustes y otros conceptos.
- Las mensualidades toman el precio acordado y el día de pago del servicio.
- Se bloquearon mensualidades futuras, anteriores a la activación y duplicadas.
- Cada cargo conserva al Customer responsable según la titularidad.
- Se implementó el resumen de deuda total, vencida y cargos abiertos.
- Los cargos erróneos pueden cancelarse sin eliminar su historial.
- Se aplicó la novena migración y el proyecto suma cuarenta y nueve pruebas.
- Se implementó la recepción de pagos con monto declarado, método, referencia,
  comprobante, titular de origen y responsable.
- Todo pago inicia pendiente y no afecta deuda por el solo hecho de recibir un
  comprobante.
- Se implementaron verificación, rechazo y cancelación con reglas terminales.
- Las diferencias entre monto declarado y confirmado exigen una explicación.
- Cada transición de pago conserva un evento de auditoría.
- Se aplicó la décima migración y el proyecto suma cincuenta y seis pruebas.
- Se implementó PaymentAllocation para distribuir pagos verificados.
- Por defecto se cubren primero los cargos más antiguos.
- Las aplicaciones dirigidas requieren una justificación.
- Los pagos parciales actualizan el saldo y estado de cada cargo.
- Los excedentes generan saldo a favor mediante CreditMovement.
- Las mensualidades consumen automáticamente el saldo a favor disponible.
- Se implementaron devoluciones auditables limitadas al saldo existente.
- Se aplicó la undécima migración y el proyecto suma sesenta y dos pruebas.
- Se implementaron prórrogas de pago con autorización y evidencia.
- Las prórrogas requieren deuda y una nueva fecha futura.
- Sólo puede existir una prórroga activa por servicio.
- Se conservaron los estados cumplida, vencida y cancelada con resolución.
- La suspensión consulta automáticamente las prórrogas vigentes.
- Se aplicó la duodécima migración y el proyecto suma sesenta y seis pruebas.
- La suspensión ahora calcula la deuda desde los cargos abiertos.
- Se exige una mensualidad vencida cuyo periodo de tolerancia haya terminado.
- El monto reportado debe coincidir con el saldo calculado.
- Cada intento conserva una fotografía auditable de los cargos evaluados.
- Se aplicó la decimotercera migración y el proyecto suma sesenta y siete
  pruebas.
- Se implementó el registro seguro de routers MikroTik sin almacenar
  credenciales.
- El control de address list funciona en simulación por defecto y exige
  habilitación explícita para ejecutar cambios reales.
- Las órdenes de suspensión, reactivación y reconciliación son idempotentes,
  verifican el resultado y conservan auditoría y reintentos.
- Se aplicó la decimocuarta migración y el proyecto suma setenta y siete
  pruebas.
- Se enlazaron las suspensiones y reactivaciones con la orden MikroTik que
  verificó el cambio real.
- Las rutas coordinadas validan primero las reglas comerciales y sólo cambian
  el estado del servicio después de confirmar la address list.
- Las simulaciones y fallos no alteran el estado comercial; los reintentos
  pueden completar la misma operación sin duplicarla.
- Se bloqueó la declaración de éxito automático sin una orden verificada y se
  conservó el modo manual para contingencias.
- Se aplicó la decimoquinta migración y el proyecto suma ochenta y tres
  pruebas.
- Se implementaron incidentes por torre, AP o servicios afectados.
- Cada afectación conserva el titular y el periodo exacto sin cambiar el
  estado comercial del servicio.
- La resolución exige causa y responsable, y calcula la duración.
- Las bonificaciones requieren autorización, no pueden duplicarse y generan
  saldo a favor auditable para el titular afectado.
- Se aplicó la decimosexta migración y el proyecto suma noventa
  pruebas.

## Lecciones aprendidas

- Customer representa a una persona y no debe contener IP, torre, AP o plan.
- APIRouter permite agrupar rutas con un prefijo y una etiqueta comunes.
- Los modelos de entrada y salida pueden ser diferentes.
- El almacenamiento en memoria sirve para validar la API, pero pierde sus
  datos al reiniciar y no sustituye una base de datos.
- Los modelos ORM representan las tablas y los esquemas Pydantic representan
  el contrato público de la API.
- Alembic permite evolucionar el esquema sin recrear manualmente la base.

## Próximo objetivo

Implementar el siguiente módulo operativo priorizado en el roadmap.
